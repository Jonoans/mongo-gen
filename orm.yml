models:
  packageName: input
  # Folder containing the package
  packagePath: examples/input
  ignoredFiles:
    - ignored.go
output:
  # What package and where to output models
  packageName: output
  packagePath: examples/output
  ignoredFiles:
    - ignored.go